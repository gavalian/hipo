#------------------------------------------------
# LaTeX makefile
#------------------------------------------------

all: hipobench.tex
	pdflatex hipobench.tex
clean:
	@rm -rf *.aux *.spl *synctex.gz *.log *.blg *.out
